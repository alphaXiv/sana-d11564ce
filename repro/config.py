"""Per-branch experiment knobs. Edit on each experiment branch; run command is fixed."""

MODE = "bench"  # "train" | "bench"
ATTN = "linear"  # "linear" | "hybrid" | "softmax"
ATTNRES = False
SEED = 0
RUN_NAME = "bench"
